タマネギ、今回の2ファイルを再比較した結論は、大筋ではほぼ一致していますが、まだ完全一致ではありません。
前回までの重大な誤解は修正されています。ただし、実装ミスにつながる記述が2か所あります。

修正が必要な箇所

 1. プロンプトの「使い回し禁止」が仕様書と矛盾

HTMLの作業メモには、

禁止：1つのプロンプト文字列を両AIで使い回すこと。

とあります。

しかし最新確定版では、2nd AI用プロンプトは、

1st AIのプロンプトを読み込み、1st AI専用部分を除去して2nd AI専用指示を追加する

という構成です。つまり、共通本文を利用すること自体は禁止されていません。

正しくは次の表現です。

1st AIと2nd AIで、完成後の送信プロンプト全文を同一にしてはならない。
共通本文・共通データは再利用可能だが、Claude用とDeepSeek用のシステム指示、出力形式、候補上限、厳選穴レース・レース指標の有無を必ず分離する。

この修正をしないと、エンジニアさんが共通処理まで二重実装してしまう可能性があります。

 1. 「59点以下＝候補外」の表現が不正確

HTMLのB-12には、

59点以下が候補外

とあります。

最新確定版の正確な意味は、

59点以下は「高配当候補外」

です。

これは、既存の5着以内候補から除外する意味ではありません。
特にシャドー期間中は、高配当総合点によって既存候補を削除・採用・入替してはいけません。

正しくは、

高配当総合点59点以下は「高配当候補外」とする。ただし、既存の最低基準点・低配当除外・回収率フィルターを通過した5着以内候補を、高配当総合点59点以下という理由だけで削除しない。シャドー期間中は分類とログ保存だけを行う。

軽微ですが明確にした方がよい箇所

HTMLの1st AI失敗時の説明は、

Flutterに何も出さない

となっています。最新仕様では、より正確には、

通常予測を公開せず、制御済みエラーを返す

です。候補を表示しない点は同じですが、システム処理としては「無反応」と「制御済みエラー」は違うため、後者に統一した方が安全です。

一致している重要項目

以下は今回、正しく修正されています。

 • 2nd AIはユニーク馬専用ではなく、全頭を独立評価する
 • 同じ過去成績・能力適性データを両AIへ渡す
 • 外部AI呼び出しはClaudeとDeepSeekの合計2回だけ
 • 1st AIは最大7頭、2nd AIは最大5頭
 • 統合後は出走頭数に応じ最大4～7頭
 • シャドー期間中も強化指標をPHP内部で算出してログ保存する
 • シャドー期間中は強化指標を候補・順位・Flutter表示へ反映しない
 • 2nd AI失敗時は1st AI単独処理へ縮退する
 • 1st AI失敗時は通常予測を公開しない
 • AI回答の上限超過をPHPで切り捨てず、応答自体を無効にする
 • 断層学習の1,000・2,000・評価500レース条件
 • 高配当強化の200～300レース条件との区別
 • 受入チェック70項目を完成判定に使う

エンジニアさんへ送る文章

今回の依頼解釈HTMLは大筋で最新確定版と一致していますが、次の2点を修正してください。

①「1つのプロンプト文字列を両AIで使い回すことは禁止」という記述は不正確です。最新仕様では、1st AI用の共通本文を基に1st専用部分を除去し、2nd AI専用指示を追加してDeepSeek用を生成します。共通本文・共通データの再利用は可能です。ただし完成後の送信プロンプト全文を同一にせず、システム指示・出力形式・候補上限・厳選穴レース・レース指標の有無を必ず分離してください。

②「高配当総合点59点以下＝候補外」ではなく、「高配当候補外」です。既存フィルターを通過した5着以内候補を、高配当総合点59点以下だけを理由に削除しないでください。特にシャドー期間中は分類・内部算出・ログ保存だけとし、既存候補の採用・除外・入替・順位・Flutter表示へ影響させないでください。

あわせて、1st AI失敗時は単に「Flutterへ何も出さない」ではなく、「通常予測を公開せず、制御済みエラーを返す」と最新仕様どおり明記してください。

実装の正規仕様はHTMLではなく、最新確定版TXT全文です。

結論として、今回はかなり近づいていますが、この2点を修正するまでは「完全一致」とは判定しない方が安全です。
