# 馬眼力 仕様書 vs 現行コード 比較・改造計画書（改訂版）

**作成日**: 2026-09-11  
**改訂日**: 2026-09-13（よっしー指摘を受けて全面修正）  
**対象仕様書**: 馬眼力_エンジニア提出用_最終確定版_固定配点・断層パターン学習強化_20260910.txt  
**対象コード**: AiController.php（/app/Http/Controllers/Api/AiController.php）

---

## ⚠️ 改訂経緯（必読）

2026-09-12版のHTMLは「方向性確認用としては合格」だが、「最新確定仕様書を完全に実装できる確定作業書としては未完成」だった。  
特に以下の3点が最新仕様書と異なる解釈になっていた：

| 相違点 | 旧HTML（誤り） | 最新仕様書（正しい） |
|--------|--------------|-------------------|
| 2nd AIの役割 | ユニーク馬評価専用に絞る | 全頭を独立評価し、一致馬も独自馬も選出する。不一致を目的にしない |
| 能力・適性評価 | 過去履歴をDeepSeekへのみ渡して採点 | Claude・DeepSeekの**両方**へ同じ全頭履歴を渡し、それぞれ自分の1回の分析内で採点 |
| 高配当総合点 | Phase 1ではログ保存、採点反映は次フェーズ | 市場妙味基礎点＋能力適性補正でPHPが直ちに内部算出。200〜300レースのシャドー中は候補・順位・Flutter表示へ一切反映しない |

**実装は必ず最新仕様書を正規仕様として行うこと。本書と最新仕様書が食い違う場合は最新仕様書を優先する。**  
**実装完了時は仕様書末尾の「エンジニア受入チェック68項目」に沿って全項目の確認結果を提出する。**

---

## 読み方の案内

各ブロックは以下の構成で書いています。

- **仕様書の定義**: 最新仕様書がどう定めているか
- **現状コード**: 今のコードがどうなっているか
- **差分**: 何が足りていないか／間違っているか
- **改造内容**: 具体的に何を直せばいいか
- **優先度**: 🔴 必須（受入チェック対象）／🟡 重要（品質・精度に影響）／🟢 シャドー（内部計算・ログのみ、Flutter未反映）

**注意**：旧HTML では B-8〜B-14 の一部を「🟡重要」または「🟢将来」と分類していたが、最新仕様書では**全て受入条件を満たすための必須対応**である。

---

---

## ブロック1【🔴 必須】出力フォーマット：「レース指標」行の欠落

### 仕様書の定義

AIの出力は3行構成が必須：
```
1行目: 厳選穴レース|1または0
2行目: レース指標|波乱度: X|下位進入度: X|大穴進入度: X
3行目以降: 馬番：X、馬名：XXX、人気順: X、6分前オッズ: X.X、おすすめ度: XX、選出理由：〜
```

「波乱度・下位進入度・大穴進入度」はAIが1〜5の整数で判定して出力する。  
この行はFlutterがパースして画面表示に使う。

### 現状コード

プロンプトの出力指示には以下の2行しかない：
```
厳選穴レース|1または0
馬番：X、馬名：XXX、人気順: X、6分前オッズ: X.X、おすすめ度: XX、選出理由：〜
```

「レース指標|波乱度: X|下位進入度: X|大穴進入度: X」が**完全に抜けている**。

### 差分

- プロンプトの出力指示に2行目が抜けている
- AIへの「波乱度・下位進入度・大穴進入度を判定する」指示も抜けている
- PHPがこの行をパースして保存・利用するロジックが存在しない

### 改造内容

1. `_getAiAnalysisPrompt()` の出力指示セクションに2行目の出力指示を追加
2. 「波乱度・下位進入度・大穴進入度の判定方法」の指示文をプロンプトに追加（仕様書の断層構造タイプ・波乱度ルール）
3. `getHorseOddsFinderAiAnalysis()` でAI回答から「レース指標|...」行を抽出して保存
4. `getHorseOddsFinderSecondAiOpinion()` でDeepSeekへ送るプロンプトからこの行の出力指示を除去
5. APIレスポンスJSONに `race_metrics` キー（`{波乱度: X, 下位進入度: X, 大穴進入度: X}`）を追加

**Flutter側**: `race_content_page.dart:205` / `functions.dart:319` の `parseRaceMetrics(analysisText)` が `analysis_text` から直接パースするため、PHP側でレスポンスに含めれば表示される。Flutter UIは実装済み。

---

---

## ブロック2【🔴 必須】1st AI（Claude）システムプロンプト

### 仕様書の定義

システムプロンプト（仕様書に全文掲載）：

> あなたは競馬オッズ分析の専門家（1st AI）です。入力された取得開始S〜発走6分前までの全頭データだけを使用し、全頭を評価してから候補を決定してください。DB・PHP算出済みの人気順、OPI、流入ランク、推定確定オッズ、断層構造タイプ、厳選穴レース条件を再計算・変更してはいけません。3分前、確定オッズ・確定人気、実着順、払戻金その他発走後情報、入力に存在しない情報を使用・推測・創作してはいけません。主目的は的中頭数ではなく長期回収率の向上です。最低基準点、低配当除外、回収率フィルター、断層位置別・人気帯別上限を順守し、上限を埋めるための追加をしてはいけません。能力・適性は時系列オッズの補強材料として評価し、能力・適性だけで候補を決めてはいけません。有料公開するため正しい日本語を使用し、ユーザープロンプトで指定されたFlutter互換フォーマット以外の前置き・後書き・見出し・補足を出力してはいけません。

仕様書の指示：「既存システムプロンプトがある場合は**併用せず、置換する**」

### 現状コード

`baganriki_brain.txt` を参照している。旧 brain.txt は「オッズ安定性アルゴリズム」に特化した内容で、新仕様書とは思想が根本的に異なる（旧：AIが自分でオッズ変動を計算して選ぶ／新：PHPが計算したDB値をAIがスコア採点する）。

### 差分

- システムプロンプトが仕様書の固定テキストではなく旧パラダイムのbrain.txt
- 「能力・適性は補強材料（0〜3点）」という制約が含まれていない

### 改造内容

1. システムプロンプトを仕様書の全文に**完全置換**（baganriki_brain.txt の内容はシステムプロンプトから外す）
2. baganriki_brain.txt の内容は「補足統計情報」としてユーザーメッセージ末尾に付加する形に変更
3. SummaryMakeBaganrikiBrain.php の生成プロンプトを「判断基準の蒸留」→「断層タイプ別・OPI帯別傾向の統計サマリー生成」に変更
4. SummaryRacesIntrospection.php のシステムプロンプトも振り返り専用の固定テキストに差し替え、brain.txtを参考情報として末尾付加

---

---

## ブロック3【🔴 必須】固定配点（信頼度60点＋妙味40点）の採点指示

### 仕様書の定義

おすすめ度 = 信頼度60点（7項目）+ 妙味40点（4項目）の**固定配点**。

**信頼度 60点**
1. 複勝支持・安定性：0〜15点
2. 単勝・複勝の継続資金流入：0〜12点
3. 単複人気差・支持差：0〜8点
4. 断層位置・時間変化・単複一致：0〜10点
5. 類似レース統計：0〜8点
6. 予測補正の維持・直前傾向：0〜4点
7. 能力・今回条件への適性：0〜3点（A=3、B=2、C=1、D=0）

**妙味 40点**
1. 推定確定配当水準：0〜15点
2. 3種類の回収率の裏付け：0〜12点
3. OPI・予測補正OPIによる市場評価：0〜8点
4. 配当と市場流入・断層構造の整合性：0〜5点

項目の追加・削除・配点変更・信頼度と妙味の間での点数移動は**禁止**。

### 差分

仕様書通りの7項目+4項目・各配点上限・採点順序の指示がプロンプトに正確に含まれているかどうかを要確認。

### 改造内容

1. `_getAiAnalysisPrompt()` の「おすすめ度計算方法」セクションを仕様書の内容（項目名・配点上限・判定基準）に完全一致させる
2. 特に「項目7：能力・適性（0〜3点）」が追加された点を確認・追加
3. 低配当時の妙味上限（推定確定複勝最小オッズ1.5倍未満→妙味小計5点以下）の記述が含まれているか確認

---

---

## ブロック4【🔴 必須】2nd AI（DeepSeek）の役割とシステムプロンプト

### 仕様書の定義

DeepSeekの**役割**：
- 1st AIと同じ確定データを使って**全頭を独立評価**し、最大5頭を選出する
- 1st AIと一致する馬を選んでも構わない（不一致を目的にしない）
- 1st AIが見落とす可能性のある角度・解釈で評価することが重要
- 「1st AIとの不一致を目的に根拠の弱い馬を選ぶ」ことは**禁止**

DeepSeekへのシステム指示：

> 「DB・PHP算出値の変更禁止、未来情報禁止、全頭評価、回収率優先、低配当除外、頭数上限は、1st AIと2nd AIの**両方**に適用される絶対ルールです。自由に遊んでいいのは確定値の解釈・指標間の重み付け・採点・候補選出についてであり、DB/PHP値の再計算は2nd AIにも禁止。」

### 現状コード（問題点）

旧HTMLでは2nd AIを「ユニーク馬評価専用に絞る」と記述していたが、これは**仕様書と異なる誤った解釈**。

また現在のDeepSeekシステムプロンプトには：
> 「しかし、それらは1st AIを縛るための規則であり、**あなたへの縛りではありません**」
という文言が含まれており、仕様書と**真逆の内容**になっている。

### 改造内容

1. `getHorseOddsFinderSecondAiOpinion()` 内のDeepSeekシステムプロンプトを仕様書の全文に置換する
2. 2nd AIは「全頭を独立評価するAI」として扱う実装にする（ユニーク馬専用ではない）

```
旧: 「これらは1st AIを縛るための規則であり、あなたへの縛りではありません」
新: 「DB・PHP算出値の変更禁止、未来情報禁止、全頭評価、回収率優先、低配当除外、頭数上限は、
    1st AIと2nd AIの両方に適用される絶対ルールです。
    自由に遊んでいい。ただし、真剣にやれ。
    独立性は確定値の解釈・指標間の重み付け・採点・候補選出で発揮する。」
```

---

---

## ブロック5【🔴 必須】2nd AIプロンプトの除去ブロック

### 仕様書の定義

1st AIのプロンプトから以下を**除去してDeepSeekへ送る**：

1. 「厳選穴レースの判定ルール」ブロック全体
2. 出力フォーマット内の「厳選穴レース|1または0」行
3. 「レース指標|波乱度: X|下位進入度: X|大穴進入度: X」行
4. 末尾の1st AI専用出力指示

**除去してはいけないもの**（維持必須）：
- DB・PHP確定値の変更禁止
- 未来情報禁止
- 全頭評価
- 回収率優先
- 低配当除外
- **おすすめ度の固定計算式（仕様書では除去しない）**
- **「このシステムの目的」（仕様書では除去しない）**
- **回収率優先・低配当除外ルール（仕様書では除去しない）**
- サンプル数評価
- 断層位置別・人気帯別上限
- 最低基準点
- 能力・適性評価
- 頭数を埋めない規則

### 差分

現状コードでは「おすすめ度計算方法」「システムの目的」「回収率優先・低配当除外ルール」を誤って除去している。

### 改造内容

1. 2nd AIへの除去ブロックを仕様書の4項目のみに変更
2. 「おすすめ度計算方法」「システムの目的」「回収率優先・低配当除外ルール」は除去しないよう修正
3. 「レース指標|...」行の出力指示を除去する処理を追加（ブロック1実装後）

---

---

## ブロック6【🔴 必須】2nd AI独自発見枠：F1〜F8フラグ判定

### 仕様書の定義

2nd AI独自発見馬の採用条件は以下の8フラグ（F1〜F8）でPHPが機械判定：

| フラグ | 条件 | 元データ | 欠損時の扱い |
|--------|------|----------|------------|
| F1 | 市場妙味基礎点Aが12点以上 | PHP算出（ブロック10が前提） | 不明（非カウント） |
| F2 | 複勝人気順位が単勝人気順位より2順位以上高い | DB値（6分前オッズ） | 不明（非カウント） |
| F3 | 複勝流入ランクが同人気帯グループ内1位または2位 | PHP算出済み | 不明（非カウント） |
| F4 | 7番人気以下かつ9分前→6分前で単複最小オッズがともに2%超低下 | DB値（時系列） | 不明（非カウント） |
| F5 | 市場妙味基礎点Eが6点以上 | PHP算出（ブロック10が前提） | 不明（非カウント） |
| F6 | 類似レース統計サンプル30件以上かつ5着以内率70%以上 | t_horse_odds_finder_similar_race_stats | 不明（非カウント） |
| F7 | 予測補正OPIが0.95以下 | PHP算出済み | 不明（非カウント） |
| F8 | 過去回収率/OPI帯別/フェーズパターン別回収率のいずれかがサンプル30件以上かつ110%以上 | DB値 | 不明（非カウント） |

**重要**：AIの選出理由の文章からキーワードを探す方法は**禁止**。必ずDB・PHP数値から判定する。

**欠損時の処理**：データが存在しない・取得できない場合は「不明（非カウント）」として true/false のカウントから除外する（0点扱いにしない）。

### 現状コード

`_mergeAiResults()` の採用条件が `evidenceKeywords` 配列の正規表現パターンによるAIテキストのキーワードマッチ方式になっている。これは仕様書が**明示的に禁止**している方法。

### 改造内容

1. `evidenceKeywords` のキーワードマッチを廃止
2. F2・F3・F4・F6・F7・F8はDB/PHP既存値から即時判定
3. F1（基礎点A）・F5（基礎点E）はブロック10の高配当基礎点算出後に実装（暫定は不明）
4. `_mergeAiResults()` の引数に各馬のDB値（複勝人気・単勝人気・流入ランク・9分前オッズ・OPI・回収率等）を追加
5. 欠損時は 不明（非カウント）として処理

---

---

## ブロック7【🔴 必須】断層タイプ別の2nd AI独自発見枠上限

### 仕様書の定義

| 断層タイプ | 独自発見枠上限 | 追加条件 |
|-----------|--------------|---------|
| A | **原則0頭** | **おすすめ度80点以上かつF1〜F8のtrueが3個以上の場合のみ最大1頭** |
| B | 最大1頭 | — |
| C | 最大1頭 | — |
| D | 最大2頭 | — |
| E | 最大2頭 | — |

### 現状コード

タイプAは「80点以上」の条件はあるが「原則0頭」になっておらず、「F1〜F8 true数3個以上」の条件もない。

### 改造内容

```php
// 変更後
if ($gapType === 'A') {
    $secondUniqueLimit = 0;  // 原則0頭
    // F1〜F8の判定後、true数 >= 3 かつ score >= 80 のときだけ 1 に昇格
    if ($flagTrueCount >= 3 && $score >= 80) {
        $secondUniqueLimit = 1;
    }
}
```

---

---

## ブロック8【🔴 必須】断層位置別・人気帯別上限の細分化

### 仕様書の定義

**1st AI最大7頭、2nd AI最大5頭**（仕様書の絶対ルール）。

**統合後の全体上限**（出走頭数別）：

| 出走頭数 | 統合後最大選出数 |
|--------|--------------|
| 8頭以下 | 4頭 |
| 9〜13頭 | 5頭 |
| 14〜15頭 | 6頭 |
| 16頭以上 | 7頭 |

**断層位置別・人気帯別上限**（PHP機械判定）：

| 断層位置 | 1〜6番人気の上限 | 7〜10番人気の上限 | 11番人気以下の上限 |
|---------|----------------|-----------------|-----------------|
| 1〜2番人気間 | 最大3頭 | 最大2頭 | 最大1頭 |
| 2〜3番人気間 | 最大4頭 | 最大2頭 | 最大1頭 |
| 3〜4番人気間 | 最大4頭 | 最大2頭 | 最大1頭 |
| 4〜5番人気間 | 最大4頭 | 最大2頭 | 最大1頭（大穴進入度1〜2なら0頭） |
| 5〜6番人気間 | 最大3頭 | 最大3頭 | 最大2頭 |
| 6番人気以降 | 最大2頭 | 最大3頭 | 最大2頭 |
| 断層なし・判定困難 | 最大3頭 | 最大3頭 | 最大2頭 |

### 差分

- 仕様書の「断層位置別」（1〜2間、2〜3間...）の細分化が未実装
- 「1〜6人気 / 7〜10人気 / 11人気以下」という3区分での上限管理が未実装
- 1st AI最大7頭・2nd AI最大5頭のチェックが未実装
- 出走頭数別の統合後全体上限が未実装

### 改造内容

1. 1st AIレスポンスパース時に7頭超の場合は上位7頭に切り捨て
2. 2nd AIレスポンスパース時に5頭超の場合は上位5頭に切り捨て
3. `_mergeAiResults()` に断層位置別・人気帯別上限のPHP機械判定を追加
4. 「主断層位置」（何番人気間に断層があるか）をPHPで計算して `_mergeAiResults()` に渡す
5. 統合後に出走頭数別の全体上限チェックを追加

---

---

## ブロック9【🔴 必須】能力・適性評価（1st AI・2nd AI共通）

### 仕様書の定義

**1st AI（Claude）・2nd AI（DeepSeek）の両方が、既存の1回の分析内で能力・適性評価を行う。**  
第3のAI、別モデル、別APIの追加は**禁止**。AI実行は合計2回のみ。

- 1st AIへのプロンプトに全出走馬の過去成績データを追加し、1st AIが自分の分析内で採点
- 2nd AIへのプロンプトにも**同一の過去成績データ**を追加し、2nd AIが自分の分析内で採点
- どちらも「既存の1回の呼び出し」に含める（追加API呼び出しなし）

**採点（100点満点・6項目）**
1. 基礎能力・クラス実績：25点
2. 近走内容・着差・相手関係：20点
3. コース・距離・芝ダート・馬場適性：20点
4. 脚質・想定展開・枠順との適合：15点
5. 上がり性能・位置取り・レース内容：10点
6. 斤量・騎手・馬体重・休養間隔などの補正：10点

評価区分：A（80〜100）B（70〜79）C（60〜69）D（59以下）

選出理由の冒頭に「能力適性:A（82点）。〜」の形式で記載。  
**能力適性D（59点以下）の扱い**：選出理由に記載は必須だが、D評価単独で除外はしない。固定配点の「項目7: 0点」として扱い、他項目のスコアで判定する。

渡すデータ：`t_horse_odds_finder_shutsuba_history` から全出走馬の直近5〜8走。

### 差分（旧HTMLの誤り）

旧HTMLでは「DeepSeekへのみ過去履歴を渡す」としていたが、**これは誤り**。  
Claude（1st AI）・DeepSeek（2nd AI）の**両方**に同じ全頭過去成績データを渡す。

### 改造内容

1. `_getAiAnalysisPrompt()` 内で各馬の直近走データをDBから取得してプロンプトに追加（1st AI用）
2. 能力・適性評価の6項目採点指示を1st AIプロンプトに追加
3. 2nd AI（DeepSeek）へのプロンプトにも**同じ過去成績データと採点指示**を追加
4. 選出理由の先頭に「能力適性:X（XX点）。」を必ず書くよう両AI出力指示に追記
5. `t_horse_odds_finder_shutsuba_history` から直近8走を馬名で取得するクエリを実装

```sql
SELECT finishing_position, num_horses, popularity, dist, condition,
       horse_weight, last_3f, fin_time_diff, grade, race_name, `date`
FROM t_horse_odds_finder_shutsuba_history
WHERE name = :horseName
ORDER BY `date` DESC
LIMIT 8;
```

---

---

## ブロック10【🟢 シャドー】高配当精度強化（市場妙味基礎点・高配当総合点）

### 仕様書の定義

**PHPが直ちに内部算出する**（計算自体はシャドー開始と同時に実装）。  
ただし200〜300レースのシャドー期間中は**候補の採用・除外・入替・順位・Flutter表示へ一切反映しない**。  
内部計算とログ保存のみ。

**市場妙味基礎点の構成（0〜80点）**

| 項目 | 説明 | 配点 |
|------|------|------|
| A | 複勝継続流入 | 0〜20点（固定） |
| B | 複勝優位性 | 0〜15点（固定） |
| C | OPI妙味 | 0〜15点（固定） |
| D | 回収率の裏付け | 0〜15点（固定） |
| E | 断層変化根拠 | 0〜15点（固定） |

**欠損時の処理**：欠損項目を0点にせず、満点（80点）の50%未満（39点以下）の場合は基礎点を「不明」として扱う。

**能力適性補正**：AI回答の能力適性グレードから算出（A=+20、B=+15、C=+10、D=+0）。

**高配当総合点** = 市場妙味基礎点 + 能力適性補正（上限100点）。

**シャドー中の制約**（厳守）：
- シャドー中は新指標をAIへ送らない
- シャドー中はFlutterの候補・順位・理由を一切変更しない
- 本番有効化後だけFlutter理由欄への追記・採点への反映が許可される

### 改造内容

1. レース実行時にPHP側で市場妙味基礎点A〜Eを算出（全馬）
2. AIのレスポンスパース後に能力適性グレードを抽出して補正点を算出
3. 高配当総合点 = 基礎点 + 補正（上限100点）を計算
4. `t_horse_odds_finder_market_score_log` にログ保存
5. **シャドー中はFlutter・候補・順位・AI送信に一切使用しない**
6. 本番有効化フラグは200〜300レース後に別途手動で設定

---

---

## ブロック11【🟢 シャドー】断層パターン学習（M1〜M4）

### 仕様書の定義

外部AIではなく、PHPから呼び出すローカルの機械学習モデル4種類：

- **M1** 上位完結モデル：5着以内が全員1〜6番人気なら1
- **M2** 下位進入モデル：5着以内に7〜10番人気が1頭以上いれば1
- **M3** 大穴進入モデル：5着以内に11番人気以下が1頭以上いれば1
- **M4** 馬別5着以内モデル：対象馬が5着以内なら1

**フェーズ管理**：

| フェーズ | レース数条件 | 動作 |
|---------|------------|------|
| Phase 1 | 1,000レース未満 | 特徴量と正解ラベルの保存のみ。AI・候補・順位・馬券・Flutterへ一切接続しない |
| Phase 2 | 1,000〜1,999レース | モデル推論開始。ただしFlutter表示・候補・順位には使わない（予備シャドー） |
| Phase 3 | 2,000レース以上かつ評価500レース以上で正式評価合格 | 正式有効化。別途明示的な有効化操作が必要 |

モデル管理：正解ラベル・時系列分割・校正・入力ハッシュ・モデルバージョン管理が必要。

### 改造内容

1. レース実行時に特徴量スナップショットを `t_horse_odds_finder_ml_snapshot` に保存
2. レース結果確定後のバッチ処理で正解ラベル（M1〜M4）を記録
3. 1,000レース未満は予測不可・AI不接続・Flutter不反映を厳守

---

---

## ブロック12【🔴 必須】PHP側の回収率ハード除外

### 仕様書の定義

PHPが機械判定する**ハード除外**の条件：
- 「3種類の回収率（過去複勝回収率・OPI帯別回収率・フェーズパターン別回収率）」を参照
- サンプル30件以上の「有効値」が2種類以上存在し、かつそのうち2種類以上が90%未満の馬 → **ハード除外**
- 有効値が2種類以上あるが90〜99%の馬 → 除外しない（AI採点の減点のみ）
- 有効値が2種類未満、欠損、「－」、0レース → 除外しない（不明として中立扱い）

### 差分

- PHP側でのハード除外が未実装
- 旧HTMLでは `race_result_payout` だけを参照と記載していたが、3種類の回収率テーブルを参照が正しい

### 改造内容

参照テーブル：
- 過去回収率：`t_horse_odds_finder_shutsuba_history` + `t_horse_odds_finder_race_result_payout` から計算
- OPI帯別：`t_horse_odds_finder_opi_recovery`
- フェーズパターン別：`t_horse_odds_finder_phase_pattern_recovery`

処理順序：`_mergeAiResults()` 呼び出し**前**に各馬の3種類の回収率を確認してハード除外リストを作成し、マージ処理でこのリストを適用する。

---

---

## ブロック13【🔴 必須】タイブレーク・最終順位決定ルール

### 仕様書の定義

**シャドー検証中の順位決定（正規）**：
1. 統合おすすめ度の降順
2. 同点なら両AI一致馬を優先
3. それでも同じなら馬番の小さい馬を優先

**シャドー検証完了後・本番有効化後の順位決定**：
高配当総合点 → 複勝継続流入点 → 回収率点 → OPI → 能力適性点 → 馬番の順（本番有効化時に実装）

**シャドー中と本番有効化後で、全く異なる固定順位方式**であることに注意。

### 差分

現状は `usort` でスコア降順のみ。同点時のタイブレークが未実装。

### 改造内容

1. `usort` の比較関数に「同点なら一致馬優先、次に馬番小さい方」を追加（シャドー中）
2. 本番有効化後の複雑なタイブレークは別途実装

---

---

## ブロック14【🔴 必須】一致馬・独自馬の表示理由とスコア算出

### 仕様書の定義

**スコア算出**：
- 一致馬（両AI一致）：（1st AIのおすすめ度 + 2nd AIのおすすめ度）÷ 2 + 5点（上限100点）
- 1st AI独自馬：1st AIのおすすめ度そのまま
- 2nd AI独自馬：2nd AIのおすすめ度そのまま

**内部スコアの精度**：小数第1位まで保持し、Flutterに返すときだけ四捨五入する。

**表示理由**：
- 一致馬 → **1st AIの選出理由原文**を使用（2nd AIの理由はログ保存のみ）
- 1st AI独自馬 → 1st AIの選出理由を使用
- 2nd AI独自馬 → 2nd AIの選出理由を使用
- **PHPが両AIの文章を連結・要約・生成することは禁止**

### 差分

スコアの +5点処理および小数保持・表示時四捨五入が実装されているか確認が必要。

### 改造内容

1. 一致馬のスコア算出を `($score1st + $score2nd) / 2 + 5` に修正（上限100）
2. 内部スコアは `float` で保持し、Flutter返却時に `round()` で整数化
3. `category: 'matched'` の `reason` = 1st AI原文、`reason_2nd` = 2nd AI原文（連結なし）を確認
4. `t_horse_odds_finder_ai_merge_result` への保存を実装（upset_race・gap_type・merged_horsesをJSON格納）

---

---

## ブロック15【🔴 必須】「厳選穴レース」の再判定タイミング

### 仕様書の定義

PHP側での再判定手順（最終候補確定後）：
1. 条件B・C・Dを優先判定（PHPで事前算出済みのフラグ）
2. 条件B・C・Dのいずれかが成立 → **0に確定**
3. B・C・D全不成立かつ最終候補に**7〜10番人気が1頭以上いる** → **1**
4. それ以外 → **0**
5. DB保存済みの「厳選穴レース」フラグを上書き

### 差分

1st AIが出力した「厳選穴レース|X」を最終候補確定後にPHPが再判定・上書きする処理が不明瞭。

### 改造内容

`_mergeAiResults()` または `getHorseOddsFinderSecondAiOpinion()` の後処理に追加。条件B・C・Dの定義は仕様書の「厳選穴レース条件」セクションに従う。

---

---

## 【新規】HTMLに記載がなかった必須仕様

よっしーの指摘により、旧HTMLに記載が不足していた仕様を以下にまとめる。全て実装必須。

### PHP処理の固定順序

以下の順序を**必ず守る**（順序変更禁止）：

```
1. 最低基準点チェック（未満の馬を除外）
2. 低配当除外チェック（推定確定複勝1.5倍未満の馬を除外）
3. 回収率フィルター（ブロック12のハード除外）
4. 断層位置別・人気帯別上限の適用（ブロック8）
5. 統合後全体上限の適用（出走頭数別）
6. 順位確定（タイブレーク適用、ブロック13）
```

### AIレスポンスのPHP検証

AIのレスポンスから馬を取り出した後、以下を**PHPが機械検証**する：
- 馬番が出走馬として存在するか（存在しない馬番は除外）
- 人気順・6分前オッズがDB値と一致しているか（AI再計算値は不採用）
- おすすめ度が0〜100の範囲内か
- 断層位置別・人気帯別上限を超えていないか

### AI失敗時の処理

- **1st AI（Claude）が失敗した場合**：予測公開を中止する（2nd AIのみでの公開は行わない）
- **2nd AI（DeepSeek）が失敗した場合**：1st AIの結果のみで処理を完了する
- **AI修復のための追加呼び出しは禁止**。AI実行は合計2回のみ（1st AIで1回、2nd AIで1回）

### 人気薄の偽流入判定

以下7条件のうち3条件以上が成立する場合は「偽流入」として採用条件から外す：
（詳細条件は仕様書の「偽流入7条件」セクションに従う）

### シャドー期間中の絶対禁止事項

ブロック10・ブロック11のシャドー期間中：
- 新指標（高配当総合点・市場妙味基礎点・M1〜M4の予測）をAIへ送らない
- Flutterの候補・順位・選出理由を変更しない
- Flutter表示・本番候補・順位に一切反映しない

### 優先度の改訂（旧HTMLからの変更）

| ブロック | 旧HTML | 改訂後 |
|--------|--------|--------|
| B-8 断層位置別上限 | 🔴必須 | 🔴必須（変更なし） |
| B-9 能力・適性評価 | 🟡重要 | **🔴必須** |
| B-10 高配当精度強化 | 🟢将来 | **🟢シャドー（即時実装・即時ログ保存、Flutter未反映）** |
| B-11 断層パターン学習 | 🟢将来 | **🟢シャドー（即時実装・データ保存、ML未接続）** |
| B-12 回収率ハード除外 | 🟡重要 | **🔴必須** |
| B-13 タイブレーク | 🟡重要 | **🔴必須** |
| B-14 統合保存・スコア算出 | 🟡重要 | **🔴必須** |

---

---

## 改造の推奨実施順序（改訂版）

```
1. ブロック4（2nd AIシステムプロンプト修正）         ← 1時間・リスク低
2. ブロック5（2nd AIの除去ブロック修正）             ← 1時間・リスク低
3. ブロック1（出力フォーマット・レース指標追加）      ← 2時間・Flutter影響あり
4. ブロック2（1st AIシステムプロンプト確認・修正）    ← 1〜2時間
5. ブロック3（固定配点採点指示の確認・修正）          ← 2〜3時間
6. ブロック9（能力・適性評価 両AIへのデータ追加）    ← 3〜5時間（★1st AI側も必要）
7. ブロック15（厳選穴レース再判定）                   ← 2時間
8. ブロック13（タイブレーク追加）                     ← 1時間
9. ブロック14（スコア算出修正・統合結果保存）         ← 2時間
10. ブロック8（断層位置別・人気帯別上限の細分化）     ← 4〜6時間
11. ブロック12（回収率ハード除外 3種類参照）          ← 2〜3時間
12. ブロック7（タイプAの独自発見枠修正）              ← 1時間（ブロック6に依存）
13. ブロック6（F1〜F8フラグ判定への変更）            ← 4〜6時間（ブロック9完了後）
14. ブロック10（高配当精度強化・シャドー即時実装）   ← 6〜12時間（ブロック9完了後）
15. ブロック11（断層パターン学習・フェーズ1）        ← 別途フェーズ管理
```

---

## 全体サマリー（改訂版）

| # | ブロック | 優先度 | 概算工数 | 備考 |
|---|---------|--------|---------|------|
| 1 | レース指標行の追加 | 🔴必須 | 2h | Flutter UI は実装済み |
| 2 | 1st AIシステムプロンプト | 🔴必須 | 1〜2h | |
| 3 | 固定配点の採点指示 | 🔴必須 | 2〜3h | |
| 4 | DeepSeekシステムプロンプト修正 | 🔴必須 | 1h | |
| 5 | 2nd AIの除去ブロック修正 | 🔴必須 | 1h | |
| 6 | F1〜F8フラグ判定 | 🔴必須 | 4〜6h | ブロック9・10完了後 |
| 7 | タイプAの独自発見枠 | 🔴必須 | 1h | ブロック6に依存 |
| 8 | 断層位置別・人気帯別上限 | 🔴必須 | 4〜6h | 1st7頭・2nd5頭制限含む |
| 9 | 能力・適性評価（両AI） | 🔴必須 | 3〜5h | ★両AIへ同一データ送信 |
| 10 | 高配当精度強化（シャドー） | 🟢シャドー | 6〜12h | 内部算出のみ・Flutter未反映 |
| 11 | 断層パターン学習（シャドー） | 🟢シャドー | 別途 | Phase1=保存のみ |
| 12 | 回収率ハード除外 | 🔴必須 | 2〜3h | 3種類の回収率参照 |
| 13 | タイブレーク修正 | 🔴必須 | 1h | シャドー中と本番で異なる |
| 14 | スコア算出修正・統合保存 | 🔴必須 | 2h | +5点・小数保持・理由ルール |
| 15 | 厳選穴レース再判定 | 🔴必須 | 2h | 条件B/C/D優先 |

🔴必須合計：約26〜35時間  
🟢シャドー：別途フェーズ管理

---

## 実装完了時の提出物

- 最新仕様書末尾の**「エンジニア受入チェック68項目」**に沿った全項目の対応表
- 各項目のテスト結果（実データで確認したもの）

---

*作成: 2026-09-11*  
*改訂: 2026-09-13（よっしー指摘 01.txt / 02.txt を受けて全面修正）*  
*本書と最新仕様書が食い違う場合は最新仕様書を正規仕様として優先すること*
